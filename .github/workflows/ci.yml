name: Alberto Test 1 Node.js CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set Up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: List Files
        run: ls

      - name: Print Working Directory
        run: pwd

      - name: List Files in ClientApp
        run: ls ClientApp

      - name: Install Dependencies
        run: |
            cd ClientApp
            npm install

      - name: Run Tests
        run: |
            cd ClientApp
            npm test
